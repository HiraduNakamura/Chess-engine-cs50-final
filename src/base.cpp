#pragma once
#define WHITE 0
#define BLACK 1
#include <vector>
#include <array>
#include <cstdint>
typedef int8_t Color;
enum ErrCode {
    ERR_OK = 0,
    ERR_INVALID_ARGS = 1,
    ERR_ILLEGAL_MOVE = 2,
};

class Base {
    //TODO: add base class stuff
};